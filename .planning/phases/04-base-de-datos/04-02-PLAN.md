---
phase: 04-base-de-datos
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/db/prisma/migrations/00000000000000_init/migration.sql
  - packages/db/prisma/seed.ts
  - packages/db/package.json
autonomous: true
requirements:
  - DB-02
  - DB-04
user_setup:
  - service: supabase
    why: "Base de datos PostgreSQL con RLS para todas las tablas"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Settings → Database → Connection string → Connection pooling (port 6543). Anadir ?pgbouncer=true&connection_limit=1"
      - name: DIRECT_URL
        source: "Supabase Dashboard → Settings → Database → Connection string → Direct connection (port 5432)"
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Settings → API → Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Settings → API → anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Settings → API → service_role key (NUNCA exponer al cliente)"

must_haves:
  truths:
    - "Las 11 tablas existen en Supabase PostgreSQL tras ejecutar la migracion"
    - "RLS esta habilitado en las 11 tablas con policies correctas"
    - "service_role puede escribir en todas las tablas"
    - "anon solo puede leer kits y plans (tablas publicas)"
    - "Seed crea 2 Kits, 6 Plans (3 por Kit), y 1 usuario test"
  artifacts:
    - path: "packages/db/prisma/migrations/00000000000000_init/migration.sql"
      provides: "Migracion inicial con schema + RLS SQL"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "packages/db/prisma/seed.ts"
      provides: "Seed data con Kits, Plans y usuario test"
      contains: "stagekit"
  key_links:
    - from: "packages/db/prisma/seed.ts"
      to: "packages/db/src/client.ts"
      via: "import prisma"
      pattern: "import.*prisma.*client"
    - from: "migration.sql"
      to: "Supabase PostgreSQL"
      via: "prisma migrate dev"
      pattern: "CREATE TABLE"
---

<objective>
Ejecutar la migracion inicial contra Supabase PostgreSQL, aplicar RLS policies en las 11 tablas via SQL raw en la migracion, y crear seed data con 2 Kits, 6 Plans y 1 usuario test.

Purpose: Las tablas con RLS son prerequisito para cualquier feature de auth, pagos o perfiles. El seed data permite verificar la DB y desbloquea desarrollo de fases 5-7.
Output: Base de datos Supabase con 11 tablas, RLS policies activas, y datos de seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/db/CLAUDE.md
@docs/03-schema-db.md
@docs/04-pagos-suscripciones.md
@.planning/phases/04-base-de-datos/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Dependencias del Plan 01 que este plan consume -->

From packages/db/src/client.ts:
```typescript
export const prisma: PrismaClient;
export { PrismaClient };
export type { Prisma } from "@prisma/client";
```

From packages/db/prisma/schema.prisma (modelos clave para seed):
```prisma
model Kit {
  id      String    @id          // slug: 'stagekit' | 'lexkit'
  name    String
  domain  String    @unique
  status  KitStatus @default(ACTIVE)
  config  Json      @default("{}")
  @@map("kits")
}

model Plan {
  id            String  @id @default(cuid())
  kitId         String
  name          String
  slug          String
  priceMonthly  Decimal @db.Decimal(10,2)
  priceSetup    Decimal @default(0) @db.Decimal(10,2)
  stripePriceId String?
  features      Json    @default("[]")
  limits        Json    @default("{}")
  @@unique([kitId, slug])
  @@map("plans")
}

model User {
  id         String   @id @default(cuid())
  supabaseId String   @unique
  email      String   @unique
  name       String?
  kitId      String
  role       UserRole @default(CLIENT)
  @@map("users")
}

enum KitStatus { ACTIVE, BETA, COMING_SOON, DEPRECATED }
enum UserRole { CLIENT, AFFILIATE, ADMIN }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Crear migracion inicial con RLS policies integradas</name>
  <files>
    packages/db/prisma/migrations/00000000000000_init/migration.sql
  </files>
  <read_first>
    packages/db/prisma/schema.prisma,
    docs/03-schema-db.md
  </read_first>
  <action>
**Estrategia:** Crear la migracion con `prisma migrate dev`, luego INYECTAR el SQL de RLS al final del migration.sql generado. Prisma no gestiona RLS nativamente, asi que el SQL de RLS debe ir como raw SQL al final de la migracion.

**1. Ejecutar la migracion inicial:**

```bash
export PATH="$HOME/.nvm/versions/node/v24.14.1/bin:$PATH"
cd packages/db
pnpm exec prisma migrate dev --name init --create-only
```

El flag `--create-only` genera el SQL sin ejecutarlo, para poder editarlo.

**2. Abrir el archivo de migracion generado** (estara en `prisma/migrations/YYYYMMDDHHMMSS_init/migration.sql`) y ANADIR al final todo el bloque SQL de RLS de `docs/03-schema-db.md` seccion "RLS Policies". El SQL exacto a anadir al final del migration.sql:

```sql
-- ============ HABILITAR RLS EN TODAS LAS TABLAS ============
ALTER TABLE public.kits ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.users ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.plans ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.subscriptions ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.client_profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.kit_profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.inbound_requests ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.affiliates ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.affiliate_payouts ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.audit_logs ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.digital_registrations ENABLE ROW LEVEL SECURITY;

-- ============ KITS (lectura publica, escritura solo service_role) ============
CREATE POLICY "kits_public_read" ON public.kits
  FOR SELECT USING (true);
CREATE POLICY "kits_service_write" ON public.kits
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ PLANS (lectura publica, escritura solo service_role) ============
CREATE POLICY "plans_public_read" ON public.plans
  FOR SELECT USING (true);
CREATE POLICY "plans_service_write" ON public.plans
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ USERS (solo su propio perfil) ============
CREATE POLICY "users_own" ON public.users
  FOR ALL TO authenticated
  USING (supabase_id = auth.uid()::text)
  WITH CHECK (supabase_id = auth.uid()::text);
CREATE POLICY "users_service" ON public.users
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ SUBSCRIPTIONS (solo su propia suscripcion) ============
CREATE POLICY "subscriptions_own" ON public.subscriptions
  FOR ALL TO authenticated
  USING (user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text))
  WITH CHECK (user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text));
CREATE POLICY "subscriptions_service" ON public.subscriptions
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ CLIENT_PROFILES (propietario full-access) ============
CREATE POLICY "client_profiles_own" ON public.client_profiles
  FOR ALL TO authenticated
  USING (user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text))
  WITH CHECK (user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text));
CREATE POLICY "client_profiles_service" ON public.client_profiles
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ KIT_PROFILES (propietario full-access, publico lectura si isPublic) ============
CREATE POLICY "kit_profiles_own" ON public.kit_profiles
  FOR ALL TO authenticated
  USING (profile_id IN (
    SELECT id FROM public.client_profiles
    WHERE user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text)
  ));
CREATE POLICY "kit_profiles_public_read" ON public.kit_profiles
  FOR SELECT USING (is_public = true);
CREATE POLICY "kit_profiles_service" ON public.kit_profiles
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ INBOUND_REQUESTS (profesional ve las suyas, publico puede INSERT) ============
CREATE POLICY "inbound_requests_own" ON public.inbound_requests
  FOR SELECT TO authenticated
  USING (profile_id IN (
    SELECT id FROM public.client_profiles
    WHERE user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text)
  ));
CREATE POLICY "inbound_requests_public_insert" ON public.inbound_requests
  FOR INSERT WITH CHECK (true);
CREATE POLICY "inbound_requests_service" ON public.inbound_requests
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ AFFILIATES (solo su propio registro de afiliado) ============
CREATE POLICY "affiliates_own" ON public.affiliates
  FOR ALL TO authenticated
  USING (user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text))
  WITH CHECK (user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text));
CREATE POLICY "affiliates_service" ON public.affiliates
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ AFFILIATE_PAYOUTS (solo sus propios payouts) ============
CREATE POLICY "affiliate_payouts_own" ON public.affiliate_payouts
  FOR SELECT TO authenticated
  USING (affiliate_id IN (
    SELECT id FROM public.affiliates
    WHERE user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text)
  ));
CREATE POLICY "affiliate_payouts_service" ON public.affiliate_payouts
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ AUDIT_LOGS (solo lectura service_role, nunca acceso directo) ============
CREATE POLICY "audit_logs_service_only" ON public.audit_logs
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============ DIGITAL_REGISTRATIONS (usuario ve las suyas) ============
CREATE POLICY "digital_registrations_own" ON public.digital_registrations
  FOR ALL TO authenticated
  USING (user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text))
  WITH CHECK (user_id IN (SELECT id FROM public.users WHERE supabase_id = auth.uid()::text));
CREATE POLICY "digital_registrations_service" ON public.digital_registrations
  FOR ALL TO service_role USING (true) WITH CHECK (true);
```

**3. Ejecutar la migracion contra Supabase:**

```bash
pnpm exec prisma migrate dev
```

Esto usa DIRECT_URL (port 5432) para la migracion y aplica tanto las tablas como las RLS policies.

**NOTA IMPORTANTE sobre nombres de columna:** Prisma genera nombres de columna en camelCase por defecto (ej: `supabaseId` → `supabase_id` en la tabla gracias a Prisma). Sin embargo, las RLS policies referencian columnas con snake_case (`supabase_id`, `user_id`, `profile_id`, `is_public`, `affiliate_id`). Verificar que los nombres de columna generados por Prisma coinciden con los usados en las policies. Si Prisma genera `supabaseId` como nombre de columna (no `supabase_id`), hay que anadir `@map("supabase_id")` en el schema para los campos referenciados en RLS:
- User.supabaseId → @map("supabase_id")  
- Subscription.userId → @map("user_id")
- ClientProfile.userId → @map("user_id")
- KitProfile.profileId → @map("profile_id")
- KitProfile.isPublic → @map("is_public")
- InboundRequest.profileId → @map("profile_id")
- Affiliate.userId → @map("user_id")
- AffiliatePayout.affiliateId → @map("affiliate_id")
- DigitalRegistration.userId → @map("user_id")

SI los @map no estan en el schema de Plan 01, ANADIRLOS antes de generar la migracion. Esto es CRITICO para que las RLS policies funcionen correctamente.

**4. Verificar las tablas y RLS en Supabase:**

```bash
# Verificar que las tablas existen
pnpm exec prisma db execute --stdin <<< "SELECT tablename FROM pg_tables WHERE schemaname='public' ORDER BY tablename;"

# Verificar RLS habilitado
pnpm exec prisma db execute --stdin <<< "SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public' AND rowsecurity=true ORDER BY tablename;"
```
  </action>
  <verify>
    <automated>cd packages/db && export PATH="$HOME/.nvm/versions/node/v24.14.1/bin:$PATH" && pnpm exec prisma migrate status 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ls packages/db/prisma/migrations/*/migration.sql (migration file exists)
    - grep -q "ENABLE ROW LEVEL SECURITY" packages/db/prisma/migrations/*/migration.sql
    - grep -q "kits_public_read" packages/db/prisma/migrations/*/migration.sql
    - grep -q "plans_public_read" packages/db/prisma/migrations/*/migration.sql
    - grep -q "users_own" packages/db/prisma/migrations/*/migration.sql
    - grep -q "subscriptions_own" packages/db/prisma/migrations/*/migration.sql
    - grep -q "client_profiles_own" packages/db/prisma/migrations/*/migration.sql
    - grep -q "kit_profiles_own" packages/db/prisma/migrations/*/migration.sql
    - grep -q "kit_profiles_public_read" packages/db/prisma/migrations/*/migration.sql
    - grep -q "inbound_requests_own" packages/db/prisma/migrations/*/migration.sql
    - grep -q "inbound_requests_public_insert" packages/db/prisma/migrations/*/migration.sql
    - grep -q "affiliates_own" packages/db/prisma/migrations/*/migration.sql
    - grep -q "affiliate_payouts_own" packages/db/prisma/migrations/*/migration.sql
    - grep -q "audit_logs_service_only" packages/db/prisma/migrations/*/migration.sql
    - grep -q "digital_registrations_own" packages/db/prisma/migrations/*/migration.sql
    - grep -c "ENABLE ROW LEVEL SECURITY" packages/db/prisma/migrations/*/migration.sql debe ser 11
  </acceptance_criteria>
  <done>
    - Migracion inicial creada con schema de 11 tablas
    - RLS habilitado en las 11 tablas (DB-02)
    - 26 policies RLS aplicadas (2-3 por tabla): public read para kits/plans, own-data para users/profiles/subscriptions, service_role write para todas
    - Migracion ejecutada contra Supabase sin errores
    - Columnas con @map("snake_case") para compatibilidad con RLS policies
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear seed script con 2 Kits, 6 Plans y usuario test</name>
  <files>
    packages/db/prisma/seed.ts,
    packages/db/package.json
  </files>
  <read_first>
    packages/db/prisma/schema.prisma,
    packages/db/src/client.ts,
    docs/04-pagos-suscripciones.md,
    packages/db/package.json
  </read_first>
  <action>
**1. Crear `packages/db/prisma/seed.ts`:**

```typescript
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

async function main() {
  console.log("Seeding database...");

  // ============ KITS ============
  const stagekit = await prisma.kit.upsert({
    where: { id: "stagekit" },
    update: {},
    create: {
      id: "stagekit",
      name: "StageKit",
      domain: "stagekit.app",
      status: "ACTIVE",
      config: {
        features: ["epk", "booking", "analytics"],
        description: "Kit digital para artistas de musica electronica",
      },
    },
  });

  const lexkit = await prisma.kit.upsert({
    where: { id: "lexkit" },
    update: {},
    create: {
      id: "lexkit",
      name: "LexKit",
      domain: "lexkit.pro",
      status: "COMING_SOON",
      config: {
        features: ["profile", "consultation", "documents"],
        description: "Kit digital para abogados y despachos",
      },
    },
  });

  console.log(`Created kits: ${stagekit.name}, ${lexkit.name}`);

  // ============ PLANS — StageKit ============
  const stagekitPlans = [
    {
      kitId: "stagekit",
      name: "Free",
      slug: "free",
      priceMonthly: 0,
      priceSetup: 0,
      stripePriceId: null,
      features: [
        "1 EPK publico",
        "Formulario de booking basico",
        "Perfil de artista",
        "Analytics basico (30 dias)",
      ],
      limits: { epks: 1, bookingsPerMonth: 10 },
    },
    {
      kitId: "stagekit",
      name: "Pro",
      slug: "pro",
      priceMonthly: 29,
      priceSetup: 0,
      stripePriceId: process.env.STRIPE_PRICE_STAGEKIT_PRO_MONTHLY ?? null,
      features: [
        "EPKs ilimitados + multitemplate",
        "Booking manager completo",
        "Analytics avanzado (12 meses)",
        "Custom domain para EPK",
        "Rider tecnico PDF generator",
        "Email sequences automaticos",
      ],
      limits: { epks: -1, bookingsPerMonth: -1 },
    },
    {
      kitId: "stagekit",
      name: "Agency",
      slug: "agency",
      priceMonthly: 199,
      priceSetup: 0,
      stripePriceId: process.env.STRIPE_PRICE_STAGEKIT_AGENCY_MONTHLY ?? null,
      features: [
        "Todo Pro",
        "Gestion multi-artista (hasta 20)",
        "White-label EPKs",
        "API access",
        "Soporte prioritario 24h",
        "Onboarding asistido",
      ],
      limits: { epks: -1, bookingsPerMonth: -1, artists: 20 },
    },
  ];

  for (const plan of stagekitPlans) {
    await prisma.plan.upsert({
      where: { kitId_slug: { kitId: plan.kitId, slug: plan.slug } },
      update: {},
      create: plan,
    });
  }

  console.log(`Created ${stagekitPlans.length} StageKit plans (Free/Pro 29EUR/Agency 199EUR)`);

  // ============ PLANS — LexKit ============
  const lexkitPlans = [
    {
      kitId: "lexkit",
      name: "Free",
      slug: "free",
      priceMonthly: 0,
      priceSetup: 0,
      stripePriceId: null,
      features: [
        "1 ficha de despacho publica",
        "Formulario de consulta basico",
        "Perfil profesional",
      ],
      limits: { profiles: 1, consultationsPerMonth: 5 },
    },
    {
      kitId: "lexkit",
      name: "Pro",
      slug: "pro",
      priceMonthly: 29,
      priceSetup: 0,
      stripePriceId: null, // LexKit aun no tiene Stripe prices
      features: [
        "Fichas ilimitadas",
        "Gestion de consultas completa",
        "Analytics avanzado (12 meses)",
        "Documentos automatizados",
      ],
      limits: { profiles: -1, consultationsPerMonth: -1 },
    },
    {
      kitId: "lexkit",
      name: "Agency",
      slug: "agency",
      priceMonthly: 199,
      priceSetup: 0,
      stripePriceId: null,
      features: [
        "Todo Pro",
        "Multi-abogado (hasta 20)",
        "White-label fichas",
        "API access",
        "Soporte prioritario",
      ],
      limits: { profiles: -1, consultationsPerMonth: -1, lawyers: 20 },
    },
  ];

  for (const plan of lexkitPlans) {
    await prisma.plan.upsert({
      where: { kitId_slug: { kitId: plan.kitId, slug: plan.slug } },
      update: {},
      create: plan,
    });
  }

  console.log(`Created ${lexkitPlans.length} LexKit plans (Free/Pro 29EUR/Agency 199EUR)`);

  // ============ TEST USER ============
  const testUser = await prisma.user.upsert({
    where: { email: "test@alexendros.me" },
    update: {},
    create: {
      supabaseId: "test-supabase-id-00000000",
      email: "test@alexendros.me",      // RGPD Art.6.1.b
      name: "Test User",                // RGPD Art.6.1.b
      kitId: "stagekit",
      role: "ADMIN",
    },
  });

  console.log(`Created test user: ${testUser.email} (role: ${testUser.role})`);

  console.log("Seed completed successfully!");
}

main()
  .then(async () => {
    await prisma.$disconnect();
  })
  .catch(async (e) => {
    console.error("Seed failed:", e);
    await prisma.$disconnect();
    process.exit(1);
  });
```

**2. Actualizar `packages/db/package.json`** para anadir la configuracion de seed en prisma:

Anadir al package.json el bloque `prisma`:
```json
{
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
```

**3. Ejecutar el seed:**

```bash
export PATH="$HOME/.nvm/versions/node/v24.14.1/bin:$PATH"
cd packages/db
pnpm exec prisma db seed
```

**4. Verificar seed data:**

```bash
pnpm exec prisma db execute --stdin <<< "SELECT id, name, domain FROM kits ORDER BY id;"
pnpm exec prisma db execute --stdin <<< "SELECT k.name as kit, p.name as plan, p.slug, p.price_monthly FROM plans p JOIN kits k ON p.kit_id = k.id ORDER BY k.name, p.price_monthly;"
pnpm exec prisma db execute --stdin <<< "SELECT email, role, kit_id FROM users;"
```
  </action>
  <verify>
    <automated>cd packages/db && export PATH="$HOME/.nvm/versions/node/v24.14.1/bin:$PATH" && pnpm exec prisma db seed 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f packages/db/prisma/seed.ts
    - grep -q "stagekit" packages/db/prisma/seed.ts
    - grep -q "lexkit" packages/db/prisma/seed.ts
    - grep -q "Free" packages/db/prisma/seed.ts
    - grep -q "Pro" packages/db/prisma/seed.ts
    - grep -q "Agency" packages/db/prisma/seed.ts
    - grep -q "29" packages/db/prisma/seed.ts (precio Pro)
    - grep -q "199" packages/db/prisma/seed.ts (precio Agency)
    - grep -q "test@alexendros.me" packages/db/prisma/seed.ts (usuario test)
    - grep -q '"seed"' packages/db/package.json (prisma seed config)
    - grep -q "upsert" packages/db/prisma/seed.ts (idempotent seed)
  </acceptance_criteria>
  <done>
    - Seed script creado con upsert (idempotente, ejecutable multiples veces)
    - 2 Kits: stagekit (ACTIVE) y lexkit (COMING_SOON) (DB-04)
    - 3 Plans por Kit: Free (0EUR), Pro (29EUR), Agency (199EUR) (DB-04)
    - 1 usuario test con email test@alexendros.me y role ADMIN (DB-04)
    - StageKit plans incluyen stripePriceId desde env vars (preparado para Phase 5)
    - prisma db seed ejecuta sin errores
    - Datos verificables con prisma studio
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RLS anon vs authenticated | anon solo lee tablas publicas (kits, plans, kit_profiles publicos); authenticated accede a sus propios datos |
| RLS authenticated vs service_role | service_role bypassa RLS para mutations backend; authenticated limitado a own-data |
| seed data scope | Seed crea datos de catalogo (Kits, Plans) y un usuario test; nunca credenciales reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Elevation of Privilege | RLS policy bypass | mitigate | 26 RLS policies verificadas; service_role solo en server context; anon limitado a SELECT en tablas publicas |
| T-04-06 | Information Disclosure | Seed data con email real | mitigate | Seed usa test@alexendros.me (no email real); supabaseId es placeholder |
| T-04-07 | Spoofing | inbound_requests_public_insert sin auth | accept | Diseño intencionado: visitantes no autenticados deben poder enviar booking requests/consultas. Honeypot anti-spam se implementa en Phase 7 |
| T-04-08 | Tampering | Migration SQL injection | accept | Migration es one-time, ejecutada por developer con DIRECT_URL; no hay input externo |
</threat_model>

<verification>
1. `pnpm exec prisma migrate status` -- migracion aplicada sin pendientes
2. `grep -c "ENABLE ROW LEVEL SECURITY" packages/db/prisma/migrations/*/migration.sql` -- debe ser 11
3. `grep -c "CREATE POLICY" packages/db/prisma/migrations/*/migration.sql` -- debe ser >= 25
4. `pnpm exec prisma db seed` -- ejecuta sin errores
5. `pnpm exec prisma studio` -- verificar datos visualmente (2 kits, 6 plans, 1 user)
</verification>

<success_criteria>
- Migracion init aplicada contra Supabase sin errores
- 11 tablas creadas con nombres @@map snake_case
- RLS habilitado en las 11 tablas (DB-02)
- 26+ policies creadas (public_read + own + service para cada tabla)
- Seed crea 2 Kits, 6 Plans (3 por Kit con precios 0/29/199 EUR), 1 usuario test (DB-04)
- Seed es idempotente (upsert, ejecutable multiples veces)
- prisma migrate status muestra 0 migraciones pendientes
</success_criteria>

<output>
After completion, create `.planning/phases/04-base-de-datos/04-02-SUMMARY.md`
</output>
