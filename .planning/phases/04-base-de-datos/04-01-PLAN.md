---
phase: 04-base-de-datos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/package.json
  - packages/db/prisma/schema.prisma
  - packages/db/src/index.ts
  - packages/db/src/client.ts
  - packages/db/src/supabase/server.ts
  - packages/db/src/supabase/browser.ts
  - packages/db/src/supabase/middleware.ts
  - packages/db/src/supabase/index.ts
  - packages/db/tsconfig.json
autonomous: true
requirements:
  - DB-01
  - DB-03
  - DB-05
  - DB-06
user_setup: []

must_haves:
  truths:
    - "Prisma schema tiene 11 modelos con relaciones correctas y compila sin errores"
    - "AuditLog tiene stripeEventId String? @unique para idempotency de webhooks"
    - "DATABASE_URL usa pooler port 6543 y DIRECT_URL usa port 5432 para migraciones"
    - "Supabase client factory exporta 3 variantes (server, browser, middleware) via @supabase/ssr"
    - "@repo/db exporta Prisma client y Supabase factory como entrypoints separados"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "11 modelos Prisma con relaciones, enums, @@map snake_case"
      contains: "model AuditLog"
    - path: "packages/db/src/client.ts"
      provides: "PrismaClient singleton export"
      exports: ["prisma", "PrismaClient"]
    - path: "packages/db/src/supabase/server.ts"
      provides: "createServerSupabaseClient factory"
      exports: ["createServerSupabaseClient"]
    - path: "packages/db/src/supabase/browser.ts"
      provides: "createBrowserSupabaseClient factory"
      exports: ["createBrowserSupabaseClient"]
    - path: "packages/db/src/supabase/middleware.ts"
      provides: "createMiddlewareSupabaseClient factory"
      exports: ["createMiddlewareSupabaseClient"]
  key_links:
    - from: "packages/db/prisma/schema.prisma"
      to: "env(DATABASE_URL) + env(DIRECT_URL)"
      via: "datasource db block"
      pattern: 'url\s+=\s+env\("DATABASE_URL"\)'
    - from: "packages/db/src/client.ts"
      to: "prisma/schema.prisma generated client"
      via: "import from @prisma/client"
      pattern: "import.*@prisma/client"
    - from: "packages/db/src/supabase/server.ts"
      to: "@supabase/ssr"
      via: "createServerClient import"
      pattern: "import.*createServerClient.*@supabase/ssr"
---

<objective>
Instalar Prisma 5 + @supabase/ssr en packages/db, crear el schema completo con 11 modelos y relaciones, generar el Prisma client, y crear la Supabase client factory SSR con 3 variantes.

Purpose: Establece la capa de datos completa del monorepo -- schema Prisma como fuente de verdad y Supabase factory como patron de acceso auth-aware para todas las apps.
Output: packages/db con schema.prisma validado, Prisma client generado, y Supabase factory exportable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/db/CLAUDE.md
@packages/config/src/env.ts
@docs/03-schema-db.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependencias e inicializar Prisma schema completo con 11 modelos</name>
  <files>
    packages/db/package.json,
    packages/db/prisma/schema.prisma,
    packages/db/src/client.ts,
    packages/db/src/index.ts,
    packages/db/tsconfig.json
  </files>
  <read_first>
    packages/db/package.json,
    packages/db/tsconfig.json,
    packages/db/src/index.ts,
    packages/config/src/env.ts,
    docs/03-schema-db.md
  </read_first>
  <action>
**1. Instalar dependencias en packages/db:**

```bash
export PATH="$HOME/.nvm/versions/node/v24.14.1/bin:$PATH"
cd packages/db
pnpm add prisma@5 @prisma/client@5 @supabase/ssr @supabase/supabase-js
```

IMPORTANTE: Pin Prisma a version 5.x (NO 6). Usar `prisma@5` y `@prisma/client@5`.

**2. Inicializar Prisma:**

```bash
pnpm exec prisma init --datasource-provider postgresql
```

**3. Crear `packages/db/prisma/schema.prisma`** con el contenido EXACTO de `docs/03-schema-db.md` seccion "Prisma Schema", con estas MODIFICACIONES obligatorias:

a) Anadir al modelo AuditLog el campo `stripeEventId` (requerido por DB-05, no esta en el spec doc):
```prisma
model AuditLog {
  id            String   @id @default(cuid())
  userId        String?
  entity        String
  entityId      String?
  action        String
  stripeEventId String?  @unique    // idempotency check para webhooks Stripe (DB-05)
  metadata      Json     @default("{}")
  createdAt     DateTime @default(now())

  @@map("audit_logs")
  @@index([userId])
  @@index([entity, entityId])
  @@index([createdAt(sort: Desc)])
  @@index([stripeEventId])
}
```

b) El datasource block debe usar EXACTAMENTE:
```prisma
datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")      // pooler port 6543 con pgbouncer=true&connection_limit=1
  directUrl = env("DIRECT_URL")        // port 5432 solo para migraciones
}
```

c) Generator block:
```prisma
generator client {
  provider = "prisma-client-js"
}
```

d) Todos los modelos del spec: Kit, User, Plan, Subscription, ClientProfile, KitProfile, InboundRequest, Affiliate, AffiliatePayout, AuditLog, DigitalRegistration.

e) Todos los enums: KitStatus, UserRole, SubStatus, RequestStatus, PayoutStatus.

f) Todos los @@map("snake_case_plural") EXACTAMENTE como en el spec.

g) Comentarios RGPD en campos con datos personales: email, name, avatarUrl del modelo User deben tener `// RGPD Art.6.1.b`.

**4. Crear `packages/db/src/client.ts`** -- Prisma client singleton:
```typescript
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["query", "error", "warn"] : ["error"],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;

export { PrismaClient };
export type { Prisma } from "@prisma/client";
```

**5. Actualizar `packages/db/src/index.ts`** para re-exportar:
```typescript
export { prisma, PrismaClient } from "./client.js";
export type { Prisma } from "./client.js";
```

**6. Actualizar `packages/db/tsconfig.json`** para incluir prisma generated types:
```json
{
  "extends": "@repo/config/tsconfig.library.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src", "node_modules/.prisma/client"]
}
```

**7. Actualizar `packages/db/package.json`** exports para separar Prisma y Supabase:
```json
{
  "exports": {
    ".": "./src/index.ts",
    "./supabase": "./src/supabase/index.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed": "tsx prisma/seed.ts",
    "postinstall": "prisma generate",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  }
}
```

Tambien anadir `tsx` como devDependency:
```bash
pnpm add -D tsx
```

**8. Generar el Prisma client:**
```bash
pnpm exec prisma generate
```

**9. Validar el schema:**
```bash
pnpm exec prisma validate
```
  </action>
  <verify>
    <automated>cd packages/db && export PATH="$HOME/.nvm/versions/node/v24.14.1/bin:$PATH" && pnpm exec prisma validate && pnpm exec prisma generate && echo "Schema valid and client generated"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "model Kit" packages/db/prisma/schema.prisma
    - grep -q "model User" packages/db/prisma/schema.prisma
    - grep -q "model Plan" packages/db/prisma/schema.prisma
    - grep -q "model Subscription" packages/db/prisma/schema.prisma
    - grep -q "model ClientProfile" packages/db/prisma/schema.prisma
    - grep -q "model KitProfile" packages/db/prisma/schema.prisma
    - grep -q "model InboundRequest" packages/db/prisma/schema.prisma
    - grep -q "model Affiliate" packages/db/prisma/schema.prisma
    - grep -q "model AffiliatePayout" packages/db/prisma/schema.prisma
    - grep -q "model AuditLog" packages/db/prisma/schema.prisma
    - grep -q "model DigitalRegistration" packages/db/prisma/schema.prisma
    - grep -q 'stripeEventId' packages/db/prisma/schema.prisma
    - grep -q '@unique' packages/db/prisma/schema.prisma | grep -q 'stripeEventId' (stripeEventId tiene @unique)
    - grep -q 'env("DATABASE_URL")' packages/db/prisma/schema.prisma
    - grep -q 'env("DIRECT_URL")' packages/db/prisma/schema.prisma
    - grep -q "prisma" packages/db/package.json (prisma esta en dependencies)
    - grep -q "@prisma/client" packages/db/package.json
    - grep -q "@supabase/ssr" packages/db/package.json
    - grep -q "exports" packages/db/package.json
    - grep -q '"./supabase"' packages/db/package.json
    - grep -q "PrismaClient" packages/db/src/client.ts
    - grep -q "prisma" packages/db/src/index.ts
  </acceptance_criteria>
  <done>
    - 11 modelos Prisma definidos con todas las relaciones del spec docs/03-schema-db.md
    - 5 enums definidos: KitStatus, UserRole, SubStatus, RequestStatus, PayoutStatus
    - AuditLog incluye stripeEventId String? @unique (DB-05)
    - datasource usa DATABASE_URL (pooler 6543) + DIRECT_URL (5432) (DB-06)
    - Prisma client generado sin errores
    - prisma validate pasa limpio
    - package.json tiene exports separados para "." (Prisma) y "./supabase" (Supabase factory)
    - @supabase/ssr y @supabase/supabase-js instalados
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear Supabase client factory SSR con 3 variantes</name>
  <files>
    packages/db/src/supabase/server.ts,
    packages/db/src/supabase/browser.ts,
    packages/db/src/supabase/middleware.ts,
    packages/db/src/supabase/index.ts
  </files>
  <read_first>
    packages/db/package.json,
    packages/config/src/env.ts,
    packages/db/src/index.ts
  </read_first>
  <action>
**Crear el directorio `packages/db/src/supabase/`** con 4 archivos.

IMPORTANTE: Usar `@supabase/ssr` (NO `@supabase/auth-helpers-nextjs` que esta deprecado). Usar patron async cookies para Next.js 16: `const cookieStore = await cookies()`.

**1. `packages/db/src/supabase/server.ts`** -- Para Server Components, Route Handlers, Server Actions:
```typescript
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";
import type { Database } from "../types.js";

/**
 * Supabase client para Server Components, Route Handlers y Server Actions.
 * Usa cookies async (Next.js 16 obligatorio).
 * 
 * @example
 * const supabase = await createServerSupabaseClient();
 * const { data: { user } } = await supabase.auth.getUser();
 */
export async function createServerSupabaseClient() {
  const cookieStore = await cookies();

  return createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll();
        },
        setAll(cookiesToSet) {
          try {
            for (const { name, value, options } of cookiesToSet) {
              cookieStore.set(name, value, options);
            }
          } catch {
            // setAll puede fallar en Server Components (read-only).
            // Es seguro ignorar: la session se refresca en middleware.
          }
        },
      },
    }
  );
}
```

**2. `packages/db/src/supabase/browser.ts`** -- Para Client Components:
```typescript
import { createBrowserClient } from "@supabase/ssr";
import type { Database } from "../types.js";

/**
 * Supabase client para Client Components ("use client").
 * Singleton: reutiliza la misma instancia en todo el cliente.
 * 
 * @example
 * const supabase = createBrowserSupabaseClient();
 * const { data: { user } } = await supabase.auth.getUser();
 */
export function createBrowserSupabaseClient() {
  return createBrowserClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

**3. `packages/db/src/supabase/middleware.ts`** -- Para proxy.ts (Next.js 16, reemplaza middleware.ts):
```typescript
import { createServerClient } from "@supabase/ssr";
import { type NextRequest, NextResponse } from "next/server";
import type { Database } from "../types.js";

/**
 * Supabase client para proxy.ts (Next.js 16).
 * IMPORTANTE: Usar getUser() (NO getSession()) para validar auth.
 * getSession() no valida el JWT y puede causar sesiones inconsistentes.
 * 
 * @example
 * export async function proxy(request: NextRequest) {
 *   const { supabase, response } = createMiddlewareSupabaseClient(request);
 *   const { data: { user } } = await supabase.auth.getUser();
 *   if (!user && isProtectedRoute(request)) {
 *     return NextResponse.redirect(new URL('/login', request.url));
 *   }
 *   return response;
 * }
 */
export function createMiddlewareSupabaseClient(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request });

  const supabase = createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll();
        },
        setAll(cookiesToSet) {
          for (const { name, value } of cookiesToSet) {
            request.cookies.set(name, value);
          }
          supabaseResponse = NextResponse.next({ request });
          for (const { name, value, options } of cookiesToSet) {
            supabaseResponse.cookies.set(name, value, options);
          }
        },
      },
    }
  );

  return { supabase, response: supabaseResponse };
}
```

**4. `packages/db/src/supabase/index.ts`** -- Barrel export:
```typescript
export { createServerSupabaseClient } from "./server.js";
export { createBrowserSupabaseClient } from "./browser.js";
export { createMiddlewareSupabaseClient } from "./middleware.js";
```

**5. Crear `packages/db/src/types.ts`** -- Placeholder para Database types (se generara desde Supabase CLI post-migration):
```typescript
/**
 * Database types placeholder.
 * Despues de ejecutar la primera migracion, generar tipos reales con:
 * npx supabase gen types typescript --project-id <id> > src/types.ts
 * 
 * Por ahora usamos un type generico que no restringe las queries
 * pero permite tipar las factories correctamente.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type Database = Record<string, never>;
```

**6. Verificar que el typecheck pasa** para toda la factory:
```bash
cd packages/db && pnpm typecheck
```

NOTA: El typecheck puede requerir que next y next/headers esten disponibles como tipos. Si da error de modulos next no encontrados, anadir `next` como peerDependency en package.json:
```json
"peerDependencies": {
  "next": ">=16.0.0"
}
```
  </action>
  <verify>
    <automated>cd packages/db && export PATH="$HOME/.nvm/versions/node/v24.14.1/bin:$PATH" && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "createServerSupabaseClient" packages/db/src/supabase/server.ts
    - grep -q "createBrowserSupabaseClient" packages/db/src/supabase/browser.ts
    - grep -q "createMiddlewareSupabaseClient" packages/db/src/supabase/middleware.ts
    - grep -q "await cookies()" packages/db/src/supabase/server.ts (patron async Next.js 16)
    - grep -q "@supabase/ssr" packages/db/src/supabase/server.ts
    - grep -q "@supabase/ssr" packages/db/src/supabase/browser.ts
    - grep -q "@supabase/ssr" packages/db/src/supabase/middleware.ts
    - grep -q "getUser" packages/db/src/supabase/middleware.ts (comentario documental sobre getUser vs getSession)
    - grep -q "createServerSupabaseClient" packages/db/src/supabase/index.ts
    - grep -q "createBrowserSupabaseClient" packages/db/src/supabase/index.ts
    - grep -q "createMiddlewareSupabaseClient" packages/db/src/supabase/index.ts
    - grep -q "Database" packages/db/src/types.ts
  </acceptance_criteria>
  <done>
    - 3 variantes Supabase client creadas con @supabase/ssr (DB-03)
    - Server variant usa async cookies() (Next.js 16 pattern)
    - Middleware variant documenta uso obligatorio de getUser() (no getSession())
    - Browser variant usa createBrowserClient singleton
    - Barrel export en supabase/index.ts
    - Database type placeholder creado para tipado
    - package.json export "./supabase" apunta a src/supabase/index.ts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon_key vs service_role | anon_key para cliente (RLS aplica), service_role para server mutations (bypass RLS) |
| client vs server | Supabase browser client nunca debe tener acceso a service_role_key |
| pooler vs direct | pooler (6543) para runtime queries, direct (5432) solo para migrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Supabase service_role_key | mitigate | service_role_key solo en server env, nunca NEXT_PUBLIC_; validado por env.ts Zod schema |
| T-04-02 | Elevation of Privilege | Prisma client bypasses RLS | mitigate | RLS aplicado via Supabase client para reads; Prisma solo para server-side mutations con service_role |
| T-04-03 | Denial of Service | Connection pool exhaustion | mitigate | connection_limit=1 en DATABASE_URL + pgbouncer=true (DB-06) |
| T-04-04 | Tampering | Direct DB access via Supabase Studio | accept | Development-only; production access restricted to service_role via API |
</threat_model>

<verification>
1. `cd packages/db && pnpm exec prisma validate` -- schema valido
2. `cd packages/db && pnpm exec prisma generate` -- client generado
3. `grep -c "model " packages/db/prisma/schema.prisma` -- debe ser 11
4. `grep "stripeEventId.*@unique" packages/db/prisma/schema.prisma` -- existe
5. `grep "createServerSupabaseClient" packages/db/src/supabase/server.ts` -- factory existe
6. `grep "createBrowserSupabaseClient" packages/db/src/supabase/browser.ts` -- factory existe
7. `grep "createMiddlewareSupabaseClient" packages/db/src/supabase/middleware.ts` -- factory existe
</verification>

<success_criteria>
- prisma validate pasa sin errores con 11 modelos y 5 enums
- prisma generate crea el client sin errores
- stripeEventId String? @unique existe en AuditLog (DB-05)
- DATABASE_URL y DIRECT_URL configurados en datasource (DB-06)
- 3 Supabase client factories creadas con @supabase/ssr (DB-03)
- package.json tiene exports "." (Prisma) y "./supabase" (Supabase)
- Typecheck pasa sin errores en packages/db
</success_criteria>

<output>
After completion, create `.planning/phases/04-base-de-datos/04-01-SUMMARY.md`
</output>
