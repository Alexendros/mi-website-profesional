---
phase: 02-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/components.json
  - packages/ui/src/lib/utils.ts
  - packages/ui/src/components/button.tsx
  - packages/ui/src/components/card.tsx
  - packages/ui/src/components/input.tsx
  - packages/ui/src/components/badge.tsx
  - packages/ui/src/components/dialog.tsx
  - packages/ui/src/components/sheet.tsx
  - packages/ui/src/components/tabs.tsx
  - packages/ui/src/components/table.tsx
  - packages/ui/src/components/avatar.tsx
  - packages/ui/src/components/dropdown-menu.tsx
  - packages/ui/src/components/sonner.tsx
  - packages/ui/src/components/form.tsx
  - packages/ui/src/components/label.tsx
  - packages/ui/src/components/separator.tsx
  - packages/ui/src/components/skeleton.tsx
  - packages/ui/src/hooks/use-mobile.tsx
  - packages/ui/src/index.ts
  - packages/config/src/ratelimit.ts
  - packages/config/package.json
  - apps/alexendros-me/components.json
  - apps/alexendros-pro/components.json
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "15 componentes shadcn/ui instalados y exportados desde @repo/ui"
    - "cn() utility funciona con clsx + tailwind-merge"
    - "components.json configurado con style radix-vega y aliases monorepo"
    - "@repo/ui importable desde cualquier app via package.json exports"
    - "ratelimit.ts skeleton creado en packages/config"
  artifacts:
    - path: "packages/ui/components.json"
      provides: "shadcn CLI config for monorepo"
      contains: "radix-vega"
    - path: "packages/ui/src/lib/utils.ts"
      provides: "cn() utility function"
      exports: ["cn"]
    - path: "packages/ui/src/components/button.tsx"
      provides: "Button component"
    - path: "packages/ui/src/index.ts"
      provides: "Barrel export of all 15+ components"
      min_lines: 15
    - path: "packages/ui/package.json"
      provides: "Package with all shadcn dependencies and granular exports"
      contains: "@repo/ui"
    - path: "packages/config/src/ratelimit.ts"
      provides: "Upstash rate limiting skeleton for Phase 6/7"
      exports: ["createRatelimit"]
  key_links:
    - from: "packages/ui/src/components/button.tsx"
      to: "packages/ui/src/lib/utils.ts"
      via: "import cn"
      pattern: 'import.*cn.*from.*utils'
    - from: "packages/ui/src/index.ts"
      to: "packages/ui/src/components/*.tsx"
      via: "barrel re-exports"
      pattern: "export.*from.*components"
    - from: "packages/ui/package.json"
      to: "packages/ui/src/components/"
      via: "exports field"
      pattern: '"./components/*"'
---

<objective>
Inicializar shadcn/ui en packages/ui/ con 15 componentes base, configurar exports monorepo y crear skeleton de ratelimit.

Purpose: Produce la libreria de componentes compartida @repo/ui consumible por todas las apps. Los componentes heredan tokens oklch del Plan 01 via CSS variables. El skeleton de ratelimit prepara la infraestructura para Phase 6/7.

Output: 15 componentes shadcn/ui, cn() utility, components.json monorepo, barrel exports, ratelimit.ts skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-CONTEXT.md
@.planning/phases/02-design-system/02-RESEARCH.md
@.planning/phases/02-design-system/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: globals.css provides CSS custom properties -->
From packages/brand/styles/globals.css:
```css
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-primary: var(--primary);
  /* ... 31 color mappings ... */
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}
```

From packages/brand/package.json:
```json
{
  "exports": {
    "./styles/globals.css": "./styles/globals.css"
  }
}
```

From apps/alexendros-me/app/globals.css:
```css
@import "../../packages/brand/styles/globals.css";
@source "../../packages/ui/src";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inicializar shadcn/ui en packages/ui con components.json y dependencies</name>
  <files>
    packages/ui/package.json
    packages/ui/components.json
    packages/ui/src/lib/utils.ts
    apps/alexendros-me/components.json
    apps/alexendros-pro/components.json
  </files>
  <read_first>
    packages/ui/package.json
    packages/ui/tsconfig.json
    packages/ui/src/index.ts
    packages/ui/CLAUDE.md
    .planning/phases/02-design-system/02-RESEARCH.md (lines 449-511 — Pattern 6 components.json + anti-patterns)
    .planning/phases/02-design-system/02-RESEARCH.md (lines 122-130 — installation commands)
    .planning/phases/02-design-system/02-RESEARCH.md (lines 432-447 — cn() utility)
  </read_first>
  <action>
**1. Instalar dependencias en packages/ui** (per D-11):
```bash
pnpm add tailwindcss class-variance-authority clsx tailwind-merge lucide-react tw-animate-css sonner react-hook-form @hookform/resolvers zod --filter=@repo/ui
```

**2. Crear `packages/ui/src/lib/utils.ts`** con cn() utility (per Pattern 5 de RESEARCH.md):
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**3. Crear `packages/ui/components.json`** (per D-07, D-08, D-09):
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "radix-vega",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "../../packages/brand/styles/globals.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "iconLibrary": "lucide",
  "aliases": {
    "components": "@repo/ui/components",
    "utils": "@repo/ui/lib/utils",
    "hooks": "@repo/ui/hooks",
    "lib": "@repo/ui/lib",
    "ui": "@repo/ui/components"
  }
}
```

IMPORTANTE: `"style": "radix-vega"` (NO "default" — renombrado en shadcn 2026, per Pitfall 5 de RESEARCH.md). `"config": ""` (vacio — Tailwind v4 CSS-first, per D-01).

**4. Crear `apps/alexendros-me/components.json`** (per Pattern 6 de RESEARCH.md):
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "radix-vega",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "../../packages/brand/styles/globals.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "iconLibrary": "lucide",
  "aliases": {
    "components": "@/components",
    "hooks": "@/hooks",
    "lib": "@/lib",
    "utils": "@repo/ui/lib/utils",
    "ui": "@repo/ui/components"
  }
}
```

**5. Crear `apps/alexendros-pro/components.json`** — identico al de alexendros-me.

**6. Actualizar `packages/ui/package.json`** — anadir exports granulares (per UI-02):
```json
{
  "name": "@repo/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./components/*": "./src/components/*.tsx",
    "./lib/utils": "./src/lib/utils.ts",
    "./hooks/*": "./src/hooks/*.tsx"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "tailwindcss": "^4.2.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.5.0",
    "lucide-react": "^1.7.0",
    "tw-animate-css": "^1.4.0",
    "sonner": "^2.0.7",
    "react-hook-form": "^7.72.0",
    "@hookform/resolvers": "^5.2.0",
    "zod": "^4.3.0"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "typescript": "^5.8.0",
    "@eslint/js": "^9.27.0",
    "typescript-eslint": "^8.33.0",
    "eslint": "^9.27.0"
  }
}
```

NOTA: Las versiones exactas de dependencies pueden variar al instalar con `pnpm add`. Usar las que pnpm resuelva, no sobreescribir. Lo importante son los exports y la presencia de todas las deps listadas en D-11.
  </action>
  <verify>
    <automated>
test -f packages/ui/components.json && grep "radix-vega" packages/ui/components.json && test -f packages/ui/src/lib/utils.ts && grep "twMerge" packages/ui/src/lib/utils.ts && grep "class-variance-authority" packages/ui/package.json && grep '"./components/*"' packages/ui/package.json && test -f apps/alexendros-me/components.json && test -f apps/alexendros-pro/components.json && echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/components.json` contiene `"style": "radix-vega"`
    - `packages/ui/components.json` contiene `"config": ""`
    - `packages/ui/components.json` contiene `"components": "@repo/ui/components"`
    - `packages/ui/src/lib/utils.ts` exporta funcion `cn` que usa `twMerge(clsx(inputs))`
    - `packages/ui/package.json` contiene `"./components/*": "./src/components/*.tsx"` en exports
    - `packages/ui/package.json` contiene `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `tw-animate-css`, `sonner` en dependencies
    - `apps/alexendros-me/components.json` contiene `"ui": "@repo/ui/components"`
    - `apps/alexendros-pro/components.json` contiene `"ui": "@repo/ui/components"`
  </acceptance_criteria>
  <done>shadcn/ui inicializado con components.json monorepo (radix-vega), cn() utility, todas las dependencies instaladas, exports granulares en package.json, components.json en ambas apps.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar 15 componentes shadcn/ui y crear barrel exports</name>
  <files>
    packages/ui/src/components/button.tsx
    packages/ui/src/components/card.tsx
    packages/ui/src/components/input.tsx
    packages/ui/src/components/badge.tsx
    packages/ui/src/components/dialog.tsx
    packages/ui/src/components/sheet.tsx
    packages/ui/src/components/tabs.tsx
    packages/ui/src/components/table.tsx
    packages/ui/src/components/avatar.tsx
    packages/ui/src/components/dropdown-menu.tsx
    packages/ui/src/components/sonner.tsx
    packages/ui/src/components/form.tsx
    packages/ui/src/components/label.tsx
    packages/ui/src/components/separator.tsx
    packages/ui/src/components/skeleton.tsx
    packages/ui/src/hooks/use-mobile.tsx
    packages/ui/src/index.ts
  </files>
  <read_first>
    packages/ui/components.json
    packages/ui/src/lib/utils.ts
    packages/ui/package.json
    packages/ui/CLAUDE.md
    .planning/phases/02-design-system/02-RESEARCH.md (lines 654-665 — shadcn CLI add commands)
    .planning/phases/02-design-system/02-CONTEXT.md (D-10 — lista de 15 componentes)
  </read_first>
  <action>
**1. Instalar 15 componentes via shadcn CLI** (per D-10). Ejecutar desde el directorio `packages/ui/`:
```bash
cd packages/ui
pnpm dlx shadcn@latest add button card input badge dialog sheet tabs table avatar dropdown-menu label separator skeleton --cwd .
pnpm dlx shadcn@latest add sonner --cwd .
pnpm dlx shadcn@latest add form --cwd .
```

Si el CLI pide confirmacion, aceptar. Si el CLI no reconoce `--cwd .`, ejecutar sin `--cwd` desde dentro de `packages/ui/`.

Los componentes se instalaran en `packages/ui/src/components/` segun los aliases de components.json. Si se instalan en una ubicacion diferente, moverlos a `src/components/`.

**VERIFICACION POST-INSTALACION:** Comprobar que cada uno de estos 15 archivos existe en `packages/ui/src/components/`:
- button.tsx, card.tsx, input.tsx, badge.tsx, dialog.tsx, sheet.tsx, tabs.tsx, table.tsx, avatar.tsx, dropdown-menu.tsx, sonner.tsx, form.tsx, label.tsx, separator.tsx, skeleton.tsx

Si el CLI tambien instala `packages/ui/src/hooks/use-mobile.tsx`, perfecto. Si no, crearlo manualmente:
```tsx
import * as React from "react";

const MOBILE_BREAKPOINT = 768;

export function useIsMobile() {
  const [isMobile, setIsMobile] = React.useState<boolean | undefined>(undefined);

  React.useEffect(() => {
    const mql = window.matchMedia(`(max-width: ${MOBILE_BREAKPOINT - 1}px)`);
    const onChange = () => setIsMobile(window.innerWidth < MOBILE_BREAKPOINT);
    mql.addEventListener("change", onChange);
    setIsMobile(window.innerWidth < MOBILE_BREAKPOINT);
    return () => mql.removeEventListener("change", onChange);
  }, []);

  return !!isMobile;
}
```

**2. Verificar que los imports de componentes usen la ruta correcta de cn():**
Cada componente debe importar cn desde `"@repo/ui/lib/utils"` (no desde rutas relativas como `"../lib/utils"`). Si shadcn CLI genera rutas relativas, esta BIEN — las relativas funcionan dentro del mismo package. Solo verificar que `cn` se importa de alguna forma valida.

**3. Crear `packages/ui/src/index.ts`** — barrel export de todos los componentes (per UI-02):
```typescript
// Components
export * from "./components/button";
export * from "./components/card";
export * from "./components/input";
export * from "./components/badge";
export * from "./components/dialog";
export * from "./components/sheet";
export * from "./components/tabs";
export * from "./components/table";
export * from "./components/avatar";
export * from "./components/dropdown-menu";
export * from "./components/sonner";
export * from "./components/form";
export * from "./components/label";
export * from "./components/separator";
export * from "./components/skeleton";

// Hooks
export { useIsMobile } from "./hooks/use-mobile";

// Utilities
export { cn } from "./lib/utils";
```

**4. Verificar que `pnpm turbo build` o `pnpm --filter=@repo/ui typecheck` no tiene errores** de TypeScript. Si hay errores de import (Radix primitives no encontradas), instalar las @radix-ui/* packages que falten:
```bash
pnpm add @radix-ui/react-dialog @radix-ui/react-dropdown-menu @radix-ui/react-tabs @radix-ui/react-avatar @radix-ui/react-label @radix-ui/react-separator @radix-ui/react-slot --filter=@repo/ui
```
(El CLI de shadcn deberia instalarlas automaticamente, pero verificar.)
  </action>
  <verify>
    <automated>
COMPS="button card input badge dialog sheet tabs table avatar dropdown-menu sonner form label separator skeleton"; COUNT=0; for c in $COMPS; do test -f "packages/ui/src/components/$c.tsx" && COUNT=$((COUNT+1)); done; echo "Components found: $COUNT/15" && test $COUNT -eq 15 && grep "export.*from.*button" packages/ui/src/index.ts && grep "export.*from.*skeleton" packages/ui/src/index.ts && grep "export.*cn" packages/ui/src/index.ts && echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Los 15 archivos .tsx existen en `packages/ui/src/components/`: button, card, input, badge, dialog, sheet, tabs, table, avatar, dropdown-menu, sonner, form, label, separator, skeleton
    - `packages/ui/src/hooks/use-mobile.tsx` existe y exporta `useIsMobile`
    - `packages/ui/src/index.ts` contiene re-exports de los 15 componentes + useIsMobile + cn
    - Cada componente importa `cn` (verificar con `grep -l "cn" packages/ui/src/components/*.tsx`)
    - `packages/ui/src/index.ts` tiene al menos 17 lineas de exports (15 components + 1 hook + 1 utility)
    - `grep -c "export" packages/ui/src/index.ts` devuelve al menos 17
  </acceptance_criteria>
  <done>15 componentes shadcn/ui instalados en packages/ui/src/components/. Barrel export en index.ts. Hook use-mobile disponible. Todos consumiendo cn() de lib/utils. @repo/ui es importable por cualquier app.</done>
</task>

<task type="auto">
  <name>Task 3: Crear skeleton ratelimit.ts y verificar build monorepo</name>
  <files>
    packages/config/src/ratelimit.ts
    packages/config/package.json
  </files>
  <read_first>
    packages/config/src/env.ts
    packages/config/package.json
    .planning/phases/02-design-system/02-RESEARCH.md (lines 611-640 — ratelimit.ts skeleton)
    .planning/phases/02-design-system/02-CONTEXT.md (D-15)
  </read_first>
  <action>
**1. Instalar @upstash/ratelimit y @upstash/redis en packages/config** (per D-15):
```bash
pnpm add @upstash/ratelimit @upstash/redis --filter=@repo/config
```

**2. Crear `packages/config/src/ratelimit.ts`** — skeleton basico para Phase 6/7 (per D-15):
```typescript
import { Ratelimit } from "@upstash/ratelimit";
import { Redis } from "@upstash/redis";

/**
 * Rate limiter factory.
 * Configurar UPSTASH_REDIS_REST_URL y UPSTASH_REDIS_REST_TOKEN antes de usar.
 * Se activara en Phase 6 (PRO-08).
 */
export function createRatelimit(config?: {
  requests?: number;
  window?: `${number} ${"s" | "m" | "h" | "d"}`;
}) {
  return new Ratelimit({
    redis: Redis.fromEnv(),
    limiter: Ratelimit.slidingWindow(
      config?.requests ?? 10,
      config?.window ?? "10 s",
    ),
    analytics: true,
    prefix: "@repo/ratelimit",
  });
}

export { Ratelimit, Redis };
```

**3. Actualizar `packages/config/package.json`** — anadir export para ratelimit:
Anadir a la seccion `"exports"`:
```json
"./ratelimit": "./src/ratelimit.ts"
```

Anadir `@upstash/ratelimit` y `@upstash/redis` a `"dependencies"` (ya instalados por pnpm add).

**4. Ejecutar verificacion completa del monorepo:**
```bash
pnpm turbo typecheck
```

Si typecheck falla, corregir errores antes de terminar. Los errores mas probables son:
- Missing Radix UI types → instalar @radix-ui/* packages faltantes
- Import path issues → ajustar rutas relativas en componentes
- react-hook-form type issues → verificar version de @hookform/resolvers
  </action>
  <verify>
    <automated>
test -f packages/config/src/ratelimit.ts && grep "createRatelimit" packages/config/src/ratelimit.ts && grep "Ratelimit" packages/config/src/ratelimit.ts && grep '"./ratelimit"' packages/config/package.json && grep "@upstash/ratelimit" packages/config/package.json && echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/config/src/ratelimit.ts` existe y exporta `createRatelimit`, `Ratelimit`, `Redis`
    - `packages/config/src/ratelimit.ts` contiene `Ratelimit.slidingWindow` con defaults 10 requests / 10 s
    - `packages/config/src/ratelimit.ts` contiene `prefix: "@repo/ratelimit"`
    - `packages/config/package.json` contiene `"./ratelimit": "./src/ratelimit.ts"` en exports
    - `packages/config/package.json` contiene `@upstash/ratelimit` en dependencies
    - `pnpm turbo typecheck` completa sin errores (o los errores son solo de env vars no presentes, no de tipos)
  </acceptance_criteria>
  <done>ratelimit.ts skeleton creado en packages/config con factory function y exports configurados. Monorepo typecheck pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI components | Componentes de presentacion sin acceso a datos sensibles |
| ratelimit.ts | Skeleton que usa Redis.fromEnv() — env vars validadas por Zod en env.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | S (Spoofing) | ratelimit.ts Redis.fromEnv() | mitigate | Las env vars UPSTASH_REDIS_REST_URL y UPSTASH_REDIS_REST_TOKEN se validan con Zod en env.ts (ya existente) antes de uso en Phase 6 |
| T-02-04 | D (Denial of Service) | ratelimit.ts | mitigate | El skeleton establece defaults conservadores (10 req/10s) que se ajustaran en Phase 6; el propio rate limiter es la mitigacion contra DoS |
</threat_model>

<verification>
1. Los 15 componentes existen como archivos .tsx en packages/ui/src/components/
2. packages/ui/src/index.ts hace barrel export de todos
3. packages/ui/components.json tiene style "radix-vega" y aliases correctos
4. packages/config/src/ratelimit.ts existe con createRatelimit
5. `pnpm turbo typecheck` pasa
</verification>

<success_criteria>
- 15 componentes shadcn/ui instalados y exportados como @repo/ui
- cn() utility disponible en @repo/ui/lib/utils
- components.json en packages/ui/ y ambas apps con configuracion monorepo
- package.json de @repo/ui con exports granulares (./components/*, ./lib/utils, ./hooks/*)
- ratelimit.ts skeleton creado en packages/config con exports
- Monorepo typecheck pasa sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-02-SUMMARY.md`
</output>
