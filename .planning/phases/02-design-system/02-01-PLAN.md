---
phase: 02-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/brand/src/tokens.ts
  - packages/brand/src/fonts.ts
  - packages/brand/src/index.ts
  - packages/brand/styles/globals.css
  - packages/brand/fonts/GeistVF.woff2
  - packages/brand/fonts/GeistMonoVF.woff2
  - packages/brand/package.json
  - apps/alexendros-me/app/globals.css
  - apps/alexendros-me/app/layout.tsx
  - apps/alexendros-me/postcss.config.mjs
  - apps/alexendros-pro/app/globals.css
  - apps/alexendros-pro/app/layout.tsx
  - apps/alexendros-pro/postcss.config.mjs
  - apps/alexendros-me/package.json
  - apps/alexendros-pro/package.json
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04

must_haves:
  truths:
    - "CSS custom properties oklch definidas en :root con 31 tokens semanticos shadcn"
    - "[data-kit='stagekit'], [data-kit='lexkit'] y [data-kit='gestkit'] overridean tokens de color"
    - "Geist Sans y Geist Mono cargadas via next/font/local con display: swap en cada app"
    - "Tailwind v4 CSS-first funciona sin tailwind.config.ts — @theme inline resuelve variables en runtime"
    - "@source directive en CSS de cada app escanea packages/ui/src para clases Tailwind"
  artifacts:
    - path: "packages/brand/styles/globals.css"
      provides: "CSS tokens oklch, @theme inline, @import tailwindcss, dark-first theme"
      contains: "@theme inline"
    - path: "packages/brand/fonts/GeistVF.woff2"
      provides: "Geist Sans variable font file"
    - path: "packages/brand/fonts/GeistMonoVF.woff2"
      provides: "Geist Mono variable font file"
    - path: "packages/brand/src/tokens.ts"
      provides: "TypeScript source of truth for oklch tokens — reconciled with globals.css values"
      exports: ["alexendrosTokens", "kitTokens", "semanticTokens"]
    - path: "apps/alexendros-me/app/globals.css"
      provides: "App-level CSS importing brand globals + @source for ui package"
      contains: "@source"
    - path: "apps/alexendros-me/postcss.config.mjs"
      provides: "PostCSS config with @tailwindcss/postcss"
  key_links:
    - from: "apps/alexendros-me/app/globals.css"
      to: "packages/brand/styles/globals.css"
      via: "@import"
      pattern: '@import "../../packages/brand/styles/globals.css"'
    - from: "apps/alexendros-me/app/layout.tsx"
      to: "packages/brand/fonts/"
      via: "next/font/local src path"
      pattern: "localFont"
    - from: "packages/brand/styles/globals.css"
      to: ":root CSS vars"
      via: "@theme inline"
      pattern: "@theme inline"
---

<objective>
Crear la base completa del sistema de diseno: tokens oklch en CSS y TypeScript, tipografia Geist, configuracion Tailwind v4 CSS-first y PostCSS por app.

Purpose: Establece la identidad visual como CSS custom properties consumibles por shadcn/ui y todos los componentes. Sin esto, la Phase 2 Plan 02 (shadcn components) no tiene fundamento de color ni tipografia.

Output: `packages/brand/styles/globals.css` con 31 tokens oklch + 3 kit overrides, fonts .woff2, apps configuradas con PostCSS y next/font/local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system/02-CONTEXT.md
@.planning/phases/02-design-system/02-RESEARCH.md

<interfaces>
<!-- Existing brand package exports (will be modified) -->
From packages/brand/src/tokens.ts:
```typescript
export const alexendrosTokens = {
  colors: { black, acidGreen, mineralGray, white },
  surfaces: { bg, card, border },
} as const;

export const kitTokens = {
  stagekit: { accent, secondary, surface, text },
  lexkit: { accent, secondary, surface, text },
  gestkit: { accent, secondary, surface, text },
} as const;
```

From packages/brand/src/fonts.ts:
```typescript
export const fontConfig = {
  sans: { family: "Geist", variable: "--font-geist-sans", display: "swap" },
  mono: { family: "Geist Mono", variable: "--font-geist-mono", display: "swap" },
} as const;
```

From packages/brand/src/index.ts:
```typescript
export { alexendrosTokens, kitTokens } from "./tokens";
export { fontConfig } from "./fonts";
```

From packages/brand/package.json — current exports:
```json
{
  "exports": {
    ".": "./src/index.ts",
    "./tokens": "./src/tokens.ts",
    "./fonts": "./src/fonts.ts"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear globals.css con tokens oklch y reconciliar tokens.ts</name>
  <files>
    packages/brand/styles/globals.css
    packages/brand/src/tokens.ts
    packages/brand/src/index.ts
    packages/brand/package.json
  </files>
  <read_first>
    packages/brand/src/tokens.ts
    packages/brand/src/index.ts
    packages/brand/package.json
    docs/02-design-system.md
    .planning/phases/02-design-system/02-RESEARCH.md (lines 199-355 — Pattern 1 globals.css completo)
    .planning/phases/02-design-system/02-CONTEXT.md
  </read_first>
  <action>
**1. Crear `packages/brand/styles/globals.css`** con el contenido EXACTO del Pattern 1 de RESEARCH.md (lineas 205-355). Este archivo contiene:
- `@import "tailwindcss";`
- `@import "tw-animate-css";`
- `@import "shadcn/tailwind.css";`
- `@custom-variant dark (&:where(.dark, .dark *));` (per D-04)
- `@theme inline { ... }` con los 31 mappings color + radius + fonts (per D-01, D-03, D-14)
- `:root { ... }` con 31 tokens oklch para dark-acid/StageKit default (per D-03, D-04)
- `[data-kit="lexkit"] { ... }` y `[data-kit="gestkit"] { ... }` overrides (per D-04, BRAND-04)
- `@layer base { ... }` con border-border y bg-background body

Valores oklch exactos para `:root` (StageKit dark-acid default):
```
--radius: 0.5rem
--background: oklch(0.09 0.005 240)
--foreground: oklch(0.95 0.005 240)
--card: oklch(0.13 0.008 240)
--card-foreground: oklch(0.95 0.005 240)
--popover: oklch(0.13 0.008 240)
--popover-foreground: oklch(0.95 0.005 240)
--primary: oklch(0.72 0.22 142)
--primary-foreground: oklch(0.09 0.005 240)
--secondary: oklch(0.17 0.008 240)
--secondary-foreground: oklch(0.95 0.005 240)
--muted: oklch(0.17 0.008 240)
--muted-foreground: oklch(0.65 0.01 240)
--accent: oklch(0.65 0.18 55)
--accent-foreground: oklch(0.09 0.005 240)
--destructive: oklch(0.60 0.22 25)
--destructive-foreground: oklch(0.95 0.005 240)
--border: oklch(0.22 0.01 240)
--input: oklch(0.22 0.01 240)
--ring: oklch(0.72 0.22 142)
--chart-1: oklch(0.72 0.22 142)
--chart-2: oklch(0.65 0.18 55)
--chart-3: oklch(0.65 0.16 240)
--chart-4: oklch(0.75 0.15 70)
--chart-5: oklch(0.60 0.22 25)
--sidebar: oklch(0.11 0.006 240)
--sidebar-foreground: oklch(0.95 0.005 240)
--sidebar-primary: oklch(0.72 0.22 142)
--sidebar-primary-foreground: oklch(0.09 0.005 240)
--sidebar-accent: oklch(0.17 0.008 240)
--sidebar-accent-foreground: oklch(0.95 0.005 240)
--sidebar-border: oklch(0.22 0.01 240)
--sidebar-ring: oklch(0.72 0.22 142)
```

Valores exactos para `[data-kit="lexkit"]`:
```
--primary: oklch(0.45 0.12 250)
--primary-foreground: oklch(0.95 0.005 250)
--accent: oklch(0.70 0.10 80)
--accent-foreground: oklch(0.12 0.01 250)
--ring: oklch(0.45 0.12 250)
--chart-1: oklch(0.45 0.12 250)
--chart-2: oklch(0.70 0.10 80)
--sidebar-primary: oklch(0.45 0.12 250)
--sidebar-ring: oklch(0.45 0.12 250)
```

Valores exactos para `[data-kit="gestkit"]`:
```
--primary: oklch(0.55 0.08 220)
--primary-foreground: oklch(0.95 0.005 220)
--accent: oklch(0.65 0.15 160)
--accent-foreground: oklch(0.13 0.005 220)
--ring: oklch(0.55 0.08 220)
--chart-1: oklch(0.55 0.08 220)
--chart-2: oklch(0.65 0.15 160)
--sidebar-primary: oklch(0.55 0.08 220)
--sidebar-ring: oklch(0.55 0.08 220)
```

**2. Reconciliar `packages/brand/src/tokens.ts`** con los valores de globals.css. Expandir para incluir los 31 tokens semanticos ademas de los colores base. Anadir nuevo export `semanticTokens` que mapea los mismos valores oklch que globals.css para que TypeScript y CSS sean identicos. La estructura debe ser:
```typescript
export const semanticTokens = {
  stagekit: { background: "oklch(0.09 0.005 240)", foreground: "oklch(0.95 0.005 240)", primary: "oklch(0.72 0.22 142)", ... },
  lexkit: { background: "oklch(0.09 0.005 240)", primary: "oklch(0.45 0.12 250)", ... },
  gestkit: { background: "oklch(0.09 0.005 240)", primary: "oklch(0.55 0.08 220)", ... },
} as const;
```
Mantener `alexendrosTokens` y `kitTokens` existentes actualizando valores para que coincidan con globals.css.

**3. Actualizar `packages/brand/src/index.ts`** para exportar `semanticTokens`:
```typescript
export { alexendrosTokens, kitTokens, semanticTokens } from "./tokens";
export { fontConfig } from "./fonts";
```

**4. Actualizar `packages/brand/package.json`** — anadir export path para styles:
```json
"exports": {
  ".": "./src/index.ts",
  "./tokens": "./src/tokens.ts",
  "./fonts": "./src/fonts.ts",
  "./styles/globals.css": "./styles/globals.css"
}
```

**IMPORTANTE:** No crear `tailwind.config.ts` en ningun lugar (per D-01). Toda configuracion via CSS @theme inline.
  </action>
  <verify>
    <automated>
grep -c "@theme inline" packages/brand/styles/globals.css && grep -c "data-kit" packages/brand/styles/globals.css && grep -c "oklch" packages/brand/styles/globals.css && grep "semanticTokens" packages/brand/src/index.ts && grep "globals.css" packages/brand/package.json && test ! -f packages/brand/tailwind.config.ts && echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/brand/styles/globals.css` existe y contiene `@import "tailwindcss"` en la primera linea de imports
    - `packages/brand/styles/globals.css` contiene `@theme inline {` con los 31 mappings --color-*
    - `packages/brand/styles/globals.css` contiene `:root {` con los 31 tokens oklch para dark-acid default
    - `packages/brand/styles/globals.css` contiene `[data-kit="lexkit"]` y `[data-kit="gestkit"]` overrides
    - `packages/brand/styles/globals.css` contiene `@custom-variant dark (&:where(.dark, .dark *));`
    - `packages/brand/styles/globals.css` contiene `@import "tw-animate-css"` y `@import "shadcn/tailwind.css"`
    - `packages/brand/src/tokens.ts` exporta `semanticTokens` con propiedades `stagekit`, `lexkit`, `gestkit`
    - `packages/brand/src/tokens.ts` contiene `oklch(0.72 0.22 142)` para stagekit.primary (matching globals.css)
    - `packages/brand/src/index.ts` contiene `export { alexendrosTokens, kitTokens, semanticTokens }`
    - `packages/brand/package.json` contiene `"./styles/globals.css": "./styles/globals.css"` en exports
    - NO existe `packages/brand/tailwind.config.ts` ni `tailwind.config.js`
    - `grep -c "oklch" packages/brand/styles/globals.css` devuelve al menos 50 (31 root + 9 lexkit + 9 gestkit + base layer)
  </acceptance_criteria>
  <done>globals.css con 31 tokens oklch, 3 kit overrides, @theme inline, @custom-variant dark. tokens.ts reconciliado y exportando semanticTokens. package.json con export de styles. Sin tailwind.config.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Configurar tipografia Geist y PostCSS + globals.css en apps</name>
  <files>
    packages/brand/fonts/GeistVF.woff2
    packages/brand/fonts/GeistMonoVF.woff2
    apps/alexendros-me/app/globals.css
    apps/alexendros-me/app/layout.tsx
    apps/alexendros-me/postcss.config.mjs
    apps/alexendros-me/package.json
    apps/alexendros-pro/app/globals.css
    apps/alexendros-pro/app/layout.tsx
    apps/alexendros-pro/postcss.config.mjs
    apps/alexendros-pro/package.json
  </files>
  <read_first>
    apps/alexendros-me/app/layout.tsx
    apps/alexendros-me/package.json
    apps/alexendros-pro/app/layout.tsx
    apps/alexendros-pro/package.json
    packages/brand/src/fonts.ts
    .planning/phases/02-design-system/02-RESEARCH.md (lines 389-430 — Pattern 4 next/font/local)
    .planning/phases/02-design-system/02-RESEARCH.md (lines 358-387 — Pattern 2 y 3 app CSS + PostCSS)
    .planning/phases/02-design-system/02-RESEARCH.md (lines 642-652 — Geist .woff2 extraction)
  </read_first>
  <action>
**1. Obtener .woff2 de Geist** (per D-13):
```bash
pnpm add geist --filter=@repo/brand -D
mkdir -p packages/brand/fonts
```
Localizar los archivos .woff2 variable dentro de `node_modules/geist/dist/fonts/` (verificar path exacto con `ls`). Copiar:
- Geist Sans Variable → `packages/brand/fonts/GeistVF.woff2`
- Geist Mono Variable → `packages/brand/fonts/GeistMonoVF.woff2`

Despues de copiar, desinstalar geist si se prefiere (o dejarlo como devDependency para futuras actualizaciones). Anadir `packages/brand/fonts/` a `.gitignore` solo si se deja geist como dep; de lo contrario, commit los .woff2.

**2. Instalar @tailwindcss/postcss** en cada app como devDependency (per D-06):
```bash
pnpm add @tailwindcss/postcss -D --filter=alexendros-me
pnpm add @tailwindcss/postcss -D --filter=alexendros-pro
```

**3. Crear `apps/alexendros-me/postcss.config.mjs`** (per D-06):
```javascript
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```

**4. Crear `apps/alexendros-pro/postcss.config.mjs`** — identico al de alexendros-me.

**5. Crear `apps/alexendros-me/app/globals.css`** (per D-05, BRAND-03):
```css
@import "../../packages/brand/styles/globals.css";
@source "../../packages/ui/src";
```
Solo 2 lineas. Todo el theming vive en brand/styles/globals.css.

**6. Crear `apps/alexendros-pro/app/globals.css`** — identico al de alexendros-me.

**7. Actualizar `apps/alexendros-me/app/layout.tsx`** (per D-12, D-14, BRAND-02):
```tsx
import localFont from "next/font/local";
import "./globals.css";
import type { Metadata } from "next";

const geistSans = localFont({
  src: "../../packages/brand/fonts/GeistVF.woff2",
  variable: "--font-geist-sans",
  display: "swap",
});

const geistMono = localFont({
  src: "../../packages/brand/fonts/GeistMonoVF.woff2",
  variable: "--font-geist-mono",
  display: "swap",
});

export const metadata: Metadata = {
  metadataBase: new URL("https://alexendros.me"),
  title: {
    default: "Alejandro Agusti — Fullstack Developer",
    template: "%s | Alexendros",
  },
  description:
    "Fullstack developer especializado en Next.js y Supabase. Fundador de KitOS. Madrid.",
  openGraph: { type: "website", locale: "es_ES", siteName: "Alexendros" },
  twitter: { card: "summary_large_image" },
  robots: { index: true, follow: true },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="es"
      className={`dark ${geistSans.variable} ${geistMono.variable}`}
      data-kit="stagekit"
    >
      <body>{children}</body>
    </html>
  );
}
```
Notas: `className="dark ..."` activa dark mode (per D-04). `data-kit="stagekit"` activa tokens StageKit default (per BRAND-04).

**8. Actualizar `apps/alexendros-pro/app/layout.tsx`** con el mismo patron pero:
- `metadataBase: new URL("https://alexendros.pro")`
- Mantener el metadata existente de KitOS
- `data-kit="stagekit"` (default — alexendros.pro es hub multi-kit, pero StageKit es el default)

**NOTA CRITICA:** Per D-12 y bug Next.js #51476, la fuente se carga en CADA layout.tsx, NO se exporta desde un package compartido.
  </action>
  <verify>
    <automated>
test -f packages/brand/fonts/GeistVF.woff2 && test -f packages/brand/fonts/GeistMonoVF.woff2 && grep "@tailwindcss/postcss" apps/alexendros-me/postcss.config.mjs && grep "@tailwindcss/postcss" apps/alexendros-pro/postcss.config.mjs && grep '@import "../../packages/brand/styles/globals.css"' apps/alexendros-me/app/globals.css && grep '@source "../../packages/ui/src"' apps/alexendros-me/app/globals.css && grep "localFont" apps/alexendros-me/app/layout.tsx && grep 'data-kit="stagekit"' apps/alexendros-me/app/layout.tsx && grep "font-geist-sans" apps/alexendros-me/app/layout.tsx && grep "localFont" apps/alexendros-pro/app/layout.tsx && echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/brand/fonts/GeistVF.woff2` existe y tiene tamano > 0 bytes
    - `packages/brand/fonts/GeistMonoVF.woff2` existe y tiene tamano > 0 bytes
    - `apps/alexendros-me/postcss.config.mjs` contiene `"@tailwindcss/postcss": {}`
    - `apps/alexendros-pro/postcss.config.mjs` contiene `"@tailwindcss/postcss": {}`
    - `apps/alexendros-me/app/globals.css` contiene `@import "../../packages/brand/styles/globals.css"`
    - `apps/alexendros-me/app/globals.css` contiene `@source "../../packages/ui/src"`
    - `apps/alexendros-pro/app/globals.css` contiene las mismas dos lineas
    - `apps/alexendros-me/app/layout.tsx` importa `localFont` de `"next/font/local"`
    - `apps/alexendros-me/app/layout.tsx` contiene `variable: "--font-geist-sans"` y `variable: "--font-geist-mono"`
    - `apps/alexendros-me/app/layout.tsx` contiene `className=` con `dark` y ambas variables de font
    - `apps/alexendros-me/app/layout.tsx` contiene `data-kit="stagekit"` en el elemento `<html>`
    - `apps/alexendros-pro/app/layout.tsx` contiene `localFont` y `data-kit="stagekit"`
    - `apps/alexendros-me/package.json` contiene `@tailwindcss/postcss` en devDependencies
  </acceptance_criteria>
  <done>Geist .woff2 en packages/brand/fonts/. PostCSS configurado en ambas apps. globals.css de cada app importa brand globals + @source para ui. Layouts actualizados con next/font/local, dark class y data-kit="stagekit".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS tokens | Los valores oklch son publicos y no contienen datos sensibles |
| Font files | .woff2 son archivos estaticos sin riesgo de seguridad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | packages/brand/styles/globals.css | accept | Tokens de color son publicos por diseno — no contienen datos sensibles |
| T-02-02 | T (Tampering) | packages/brand/fonts/*.woff2 | accept | Font files son estaticos y servidos via CDN con integridad de Vercel; SRI no aplica a @font-face |
</threat_model>

<verification>
1. `grep -c "@theme inline" packages/brand/styles/globals.css` devuelve 1
2. `grep -c "data-kit" packages/brand/styles/globals.css` devuelve al menos 2 (lexkit + gestkit)
3. `test -f packages/brand/fonts/GeistVF.woff2` OK
4. `grep "localFont" apps/alexendros-me/app/layout.tsx` devuelve match
5. `grep '@source "../../packages/ui/src"' apps/alexendros-me/app/globals.css` devuelve match
6. NO existe tailwind.config.ts en ningun directorio
</verification>

<success_criteria>
- packages/brand/styles/globals.css define los 31 tokens oklch con @theme inline y 3 kit overrides
- tokens.ts exporta semanticTokens reconciliados con los mismos valores oklch
- Geist Sans y Mono como .woff2 en packages/brand/fonts/
- Ambas apps (alexendros-me, alexendros-pro) tienen PostCSS, globals.css y layout.tsx configurados con fonts, dark mode y data-kit
- Zero tailwind.config.ts en el monorepo
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-01-SUMMARY.md`
</output>
